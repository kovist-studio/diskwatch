{
  "version": 1,
  "schema": {
    "id": "Stable identifier. Never reused, never renamed once shipped.",
    "label": "Short name shown in the UI.",
    "platform": "darwin | win32",
    "path": "Absolute path. ~ is the home directory. %NAME% is a Windows environment variable. A * matches one path segment; a target only matches where the whole path exists, so a glob cannot widen into a parent.",
    "description": "Plain sentence: what it is, and what happens after it is gone.",
    "risk": "safe = regenerated without asking, holds nothing a person authored. caution = holds user data, or costs real time or bandwidth to rebuild.",
    "defaultEnabled": "Whether the entry is selected when the list is first shown. Only safe entries are ever true.",
    "requiresAppClosed": "Applications that must not be running. Empty when none apply.",
    "requiresAdmin": "Needs rights DiskWatch does not ask for. Present only when true.",
    "minAgeDays": "Only files last modified more than this many days ago are eligible. Present only when the entry is age-filtered.",
    "method": "trash = hand the path to the trash package. emptyTrash = the platform's own empty-the-bin call, which is permanent and cannot use the trash package. Absent means trash.",
    "exclude": "Paths under this target that must never be touched, even when the glob matches them.",
    "note": "Something the implementer has to know that the user-facing description should not carry.",
    "confirm": "Confirmation contract for an irreversible entry. Present only on emptyTrash entries. mustState names the facts the dialog has to show; mustContainWord is literal copy that has to appear. A UI that cannot satisfy it must not offer the entry.",
    "expand": "Expansion contract. When present, the target is NOT selectable as a single unit — the UI must resolve it into the listed items first, and each is chosen separately. wholeTargetSelectable is always false here; that is the point of the field. ifUnsupported says what to do when phase 2 cannot implement it, and \"omit\" means the entry does not ship at all rather than degrading to a folder-level checkbox."
  },
  "targets": [
    {
      "id": "macos-trash",
      "label": "Trash",
      "platform": "darwin",
      "path": "~/.Trash",
      "description": "Files you have already deleted. Emptying the Trash is permanent — this is the one thing here that cannot be undone.",
      "risk": "caution",
      "defaultEnabled": false,
      "requiresAppClosed": [],
      "method": "emptyTrash",
      "note": "Cannot go through the trash package: you cannot trash the Trash. Needs the Finder/NSFileManager empty call. This is the only permanent deletion in the app and it needs its own confirmation. Per-volume bins at /Volumes/*/.Trashes are deliberately not included — external drives are somebody else's storage. Permitted by the single documented exception in CLAUDE.md, which covers these two ids and no others. It must not share a code path with trash-based cleanup: a separate function, not a flag on the existing one.",
      "confirm": {
        "style": "permanent",
        "mustState": [
          "itemCount",
          "totalSize"
        ],
        "mustContainWord": "permanently",
        "separateCodePath": true,
        "excludeFromSelectAll": true
      }
    },
    {
      "id": "macos-user-caches",
      "label": "Application caches",
      "platform": "darwin",
      "path": "~/Library/Caches/*",
      "description": "Working files apps keep to avoid redoing work. Apps rebuild them as they run; the first launch after this will be slower.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": [],
      "exclude": [
        "~/Library/Caches/JetBrains",
        "~/Library/Caches/pip",
        "~/Library/Caches/Homebrew"
      ],
      "note": "The JetBrains exclusion is not caution, it is a verified landmine: ~/Library/Caches/JetBrains/<product>/LocalHistory is PyCharm and IntelliJ Local History — per-file edit history, which is exactly what someone reaches for when they did not commit. It is user-authored work filed under Caches. pip and Homebrew are excluded only because they are listed separately below; without that they would be counted and deleted twice."
    },
    {
      "id": "xcode-derived-data",
      "label": "Xcode DerivedData",
      "platform": "darwin",
      "path": "~/Library/Developer/Xcode/DerivedData",
      "description": "Build intermediates. Xcode regenerates these; the next build will be slower.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": [
        "Xcode"
      ]
    },
    {
      "id": "coresimulator-caches",
      "label": "iOS Simulator caches",
      "platform": "darwin",
      "path": "~/Library/Developer/CoreSimulator/Caches",
      "description": "Prepared simulator data that Xcode rebuilds on demand. Your simulators and anything installed on them are untouched.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": [
        "Xcode",
        "Simulator"
      ],
      "note": "Caches only. CoreSimulator/Devices is excluded on purpose — it holds the simulators themselves and every app and file installed on them, which is state a developer can lose work in. Profiles/Runtimes is also excluded: re-downloadable, but multi-gigabyte downloads over someone else's connection is not a saving we get to make for them."
    },
    {
      "id": "npm-cache",
      "label": "npm cache",
      "platform": "darwin",
      "path": "~/.npm/_cacache",
      "description": "Packages npm saved so it would not have to download them twice. The next install re-downloads what it needs.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": []
    },
    {
      "id": "pip-cache-macos",
      "label": "pip cache",
      "platform": "darwin",
      "path": "~/Library/Caches/pip",
      "description": "Python packages pip saved so it would not have to download them twice. The next install re-downloads what it needs.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": []
    },
    {
      "id": "cargo-registry-cache-macos",
      "label": "Cargo registry cache",
      "platform": "darwin",
      "path": "~/.cargo/registry/cache",
      "description": "Downloaded Rust crate archives. Cargo fetches them again when a build needs them.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": [],
      "note": "The downloaded .crate archives only. registry/src holds the extracted sources and is left alone: Cargo rebuilds it from these archives, and deleting both makes every project rebuild from the network instead of from disk."
    },
    {
      "id": "homebrew-cache-macos",
      "label": "Homebrew cache",
      "platform": "darwin",
      "path": "~/Library/Caches/Homebrew",
      "description": "Installers Homebrew downloaded and no longer needs. It downloads them again if you reinstall something.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": []
    },
    {
      "id": "ios-backups",
      "label": "iPhone and iPad backups",
      "platform": "darwin",
      "path": "~/Library/Application Support/MobileSync/Backup",
      "description": "Full backups of devices synced to this Mac. These are the copy you would restore from if a device were lost or replaced.",
      "risk": "caution",
      "defaultEnabled": false,
      "requiresAppClosed": [
        "Finder",
        "iTunes"
      ],
      "note": "Irreplaceable user data: this is the copy someone restores from when a device is lost or replaced. The expand contract above is a requirement, not advice. Each subfolder is one device backup keyed by device id. Offering the folder as a single checkbox is forbidden, and if the per-device listing cannot be built, ifUnsupported=omit means this entry does not ship.",
      "expand": {
        "required": true,
        "unit": "deviceBackup",
        "groupBy": "deviceId",
        "mustDisplay": [
          "deviceName",
          "backupDate"
        ],
        "offerWhen": "newerBackupExistsForSameDevice",
        "wholeTargetSelectable": false,
        "ifUnsupported": "omit"
      }
    },
    {
      "id": "downloads-old-macos",
      "label": "Downloads older than 90 days",
      "platform": "darwin",
      "path": "~/Downloads",
      "description": "Files you downloaded more than 90 days ago and have not opened since. These are your files — look before you agree.",
      "risk": "caution",
      "defaultEnabled": false,
      "requiresAppClosed": [],
      "minAgeDays": 90,
      "note": "Age-filtered per file, never the folder. Use the later of modification and last-access time: an installer kept deliberately and opened last week must not read as 90 days old because it was downloaded a year ago."
    },
    {
      "id": "windows-recycle-bin",
      "label": "Recycle Bin",
      "platform": "win32",
      "path": "C:\\$Recycle.Bin",
      "description": "Files you have already deleted. Emptying the Recycle Bin is permanent — this is the one thing here that cannot be undone.",
      "risk": "caution",
      "defaultEnabled": false,
      "requiresAppClosed": [],
      "method": "emptyTrash",
      "note": "Same problem as the macOS Trash: it cannot go through the trash package. Needs SHEmptyRecycleBin. Never enumerate or delete inside C:\\$Recycle.Bin directly — the per-SID layout is undocumented and writing into it corrupts the bin for that user. Permitted by the single documented exception in CLAUDE.md, which covers these two ids and no others. It must not share a code path with trash-based cleanup: a separate function, not a flag on the existing one.",
      "confirm": {
        "style": "permanent",
        "mustState": [
          "itemCount",
          "totalSize"
        ],
        "mustContainWord": "permanently",
        "separateCodePath": true,
        "excludeFromSelectAll": true
      }
    },
    {
      "id": "windows-user-temp",
      "label": "Temporary files",
      "platform": "win32",
      "path": "%TEMP%",
      "description": "Scratch files programs left behind. Anything still in use is skipped.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": [],
      "note": "Files held open by a running process are locked and will fail to move. Expect per-file failures as normal and report them as skipped, not as an error. An installer mid-run keeps its working state here, which is a second reason a locked file must never be forced."
    },
    {
      "id": "windows-chrome-cache",
      "label": "Google Chrome cache",
      "platform": "win32",
      "path": "%LOCALAPPDATA%\\Google\\Chrome\\User Data\\*\\Cache",
      "description": "Pages and images Chrome saved so they load faster. Chrome rebuilds it as you browse. You stay signed in.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": [
        "Google Chrome"
      ],
      "note": "The glob covers every profile — Default, Profile 1 and so on — and matches only directories that actually contain a Cache subfolder, so it cannot widen into User Data itself. Cache only: Cookies, Login Data and Local Storage are sessions and saved passwords and are never touched."
    },
    {
      "id": "windows-edge-cache",
      "label": "Microsoft Edge cache",
      "platform": "win32",
      "path": "%LOCALAPPDATA%\\Microsoft\\Edge\\User Data\\*\\Cache",
      "description": "Pages and images Edge saved so they load faster. Edge rebuilds it as you browse. You stay signed in.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": [
        "Microsoft Edge"
      ]
    },
    {
      "id": "windows-firefox-cache",
      "label": "Firefox cache",
      "platform": "win32",
      "path": "%LOCALAPPDATA%\\Mozilla\\Firefox\\Profiles\\*\\cache2",
      "description": "Pages and images Firefox saved so they load faster. Firefox rebuilds it as you browse. You stay signed in.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": [
        "Firefox"
      ],
      "note": "Firefox splits its profile: the cache is under LOCALAPPDATA, while the profile that holds history and logins is under APPDATA. Only the LOCALAPPDATA side is listed here, which is why this path cannot reach anything a person would miss."
    },
    {
      "id": "npm-cache-windows",
      "label": "npm cache",
      "platform": "win32",
      "path": "%LOCALAPPDATA%\\npm-cache",
      "description": "Packages npm saved so it would not have to download them twice. The next install re-downloads what it needs.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": [],
      "note": "npm's default location on Windows. A machine with a cache= line in .npmrc keeps it elsewhere and this path will simply be absent, which is the correct outcome — do not go looking for the configured one."
    },
    {
      "id": "pip-cache-windows",
      "label": "pip cache",
      "platform": "win32",
      "path": "%LOCALAPPDATA%\\pip\\Cache",
      "description": "Python packages pip saved so it would not have to download them twice. The next install re-downloads what it needs.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": []
    },
    {
      "id": "cargo-registry-cache-windows",
      "label": "Cargo registry cache",
      "platform": "win32",
      "path": "%USERPROFILE%\\.cargo\\registry\\cache",
      "description": "Downloaded Rust crate archives. Cargo fetches them again when a build needs them.",
      "risk": "safe",
      "defaultEnabled": true,
      "requiresAppClosed": []
    },
    {
      "id": "downloads-old-windows",
      "label": "Downloads older than 90 days",
      "platform": "win32",
      "path": "%USERPROFILE%\\Downloads",
      "description": "Files you downloaded more than 90 days ago and have not opened since. These are your files — look before you agree.",
      "risk": "caution",
      "defaultEnabled": false,
      "requiresAppClosed": [],
      "minAgeDays": 90,
      "note": "Age-filtered per file, never the folder. Same rule as macOS: use the later of modification and last-access time."
    }
  ],
  "excluded": [
    {
      "path": "C:\\Windows.old",
      "reason": "Excluded by instruction, and the reasoning holds: it is the only copy of the previous Windows install and of any user file that did not survive the upgrade. Windows removes it on its own after ten days. Deleting it wrongly is unrecoverable, and it needs handling this list cannot express."
    },
    {
      "path": "C:\\Windows\\Temp",
      "reason": "Needs administrator rights, which DiskWatch does not ask for anywhere. It would fail on every non-elevated run, which is most of them. Revisit only if the app ever gains an elevation story — and the security audit deliberately does not have one. Confirmed excluded: two cache folders do not justify building an elevation story the app has avoided everywhere else."
    },
    {
      "path": "C:\\Windows\\SoftwareDistribution\\Download",
      "reason": "Needs administrator rights, and doing it correctly means stopping the Windows Update service first. Stopping a service is a system change, not a deletion, and it is outside what a disk cleaner should do. Clearing it during an update leaves Windows Update needing a repair. Confirmed excluded: two cache folders do not justify building an elevation story the app has avoided everywhere else."
    },
    {
      "path": "~/Library/Developer/CoreSimulator/Devices",
      "reason": "Holds the simulators themselves and every app and file installed on them. Recreating a device is easy; recovering what was inside one is not."
    },
    {
      "path": "~/Library/Caches/JetBrains",
      "reason": "Contains LocalHistory, which is PyCharm and IntelliJ per-file edit history — user-authored work that happens to be filed under Caches. Verified present on the development machine."
    },
    {
      "path": "/Volumes/*/.Trashes",
      "reason": "Per-volume bins on external and network drives. DiskWatch has no way to know whose drive it is or what else uses it."
    },
    {
      "path": "~/Library/Logs",
      "reason": "Not researched. Mostly noise, but crash reports here are sometimes the only record of why something failed, and the saving is small."
    }
  ]
}
